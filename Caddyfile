{
    # Désactiver HTTPS automatique pour Railway
    auto_https off
}

:80 {
    root * /usr/share/caddy
    encode gzip
    
    # Configuration SPA - réécrire toutes les requêtes qui ne correspondent pas à des fichiers existants
    try_files {path} /index.html
    
    file_server
}
